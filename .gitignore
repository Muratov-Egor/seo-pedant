node_modules/
.DS_Store
logs/*.log

# Артефакты прогонов живут только локально: сохранённые страницы весят по ~600 КБ,
# а run.json — сотни килобайт на прогон. Нужны, чтобы прогнать новую проверку по
# прошлым страницам (node lib/check.mjs --run <runId>), но не в репозитории.
# История проблем между прогонами хранится не здесь, а в data/history.ndjson.
data/runs/
data/cache/

# Секреты появятся, когда доставку возьмёт на себя Slack App
.env

# Playwright MCP scratch output
.playwright-mcp/
